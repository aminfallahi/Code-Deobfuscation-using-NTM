from __future__ import absolute_import,unicode_literals
B=None
from wagtail.wagtailsearch.backends import get_search_backend as A
class C:
	def search(B,query_string,fields=B,operator=B,order_by_relevance=True,backend='default'):'\n        This runs a search query on all the items in the QuerySet\n        ';C=A(backend);return C.search(query_string,B,fields=fields,operator=operator,order_by_relevance=order_by_relevance)