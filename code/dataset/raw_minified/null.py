from __future__ import absolute_import
from behave.formatter.base import Formatter as A
class B(A):'\n    Provides formatter that does not output anything.\n    Implements the NULL pattern for a formatter (similar like: /dev/null).\n    ';name='null';description='Provides formatter that does not output anything.'