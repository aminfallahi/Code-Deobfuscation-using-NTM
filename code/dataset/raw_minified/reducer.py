import sys,json
from operator import itemgetter
from framework import Reducer as A
class B(A):
	def reduce(A):
		for (B,C) in A:A.emit(B,sum((float(A[1])for A in C)))
if __name__=='__main__':C=B(sys.stdin);C.reduce()