import logging as A
B=A.getLogger('evolution')
def C(root,registry):
	C='created';B.info('substanced evolve step 10: add created index');A=root['catalogs']['system'];A.update_indexes()
	if A.get(C):A.reindex(indexes=(C,))