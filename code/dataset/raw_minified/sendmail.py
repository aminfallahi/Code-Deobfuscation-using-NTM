import smtplib
from smtp import MailConnection as A
class B(A):
	def login(A):0
	def get_connection(A):0
	def send_mail(A,from_,to_,message):from subprocess import Popen,PIPE;B=Popen([A.sendmail_location,'-t'],stdin=PIPE);B.communicate(str(message))
	def close(A):0